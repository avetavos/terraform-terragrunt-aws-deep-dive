---
title: 'Module Versioning and Registries'
description: 'ทำไมการ pin version ของ module ถึงสำคัญ pessimistic constraint operator ทำงานอย่างไร และ private module source เหมาะกับกรณีไหน'
sidebar:
  order: 3
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## ไอเดียในหนึ่งประโยค

module `source` ที่ไม่ได้ pin version ไว้ สามารถ resolve ไปเป็น version ใหม่กว่าเดิมแบบเงียบ ๆ ในครั้งถัดไปที่มีคนรัน `init` ดังนั้นการ pin `version` constraint คือวินัยแบบเดียวกับที่คุณใช้กับ provider lock file อยู่แล้ว เพียงแค่เล็งไปที่ module code แทน provider code

## ทำไม module version ที่ไม่ pin ถึงเป็นความเสี่ยงแบบเงียบ

module ที่มาจาก Registry หรืออ้างอิงผ่าน Git tag ไม่ได้ตายตัวเหมือน local relative path ถ้า `module` block ไม่มี `version` constraint การรัน `terraform init -upgrade` — หรือแม้แต่ `init` ครั้งแรกใน CI pipeline ที่ยังไม่เคยรันมาก่อน — ก็สามารถดึง version ใหม่กว่าของ module นั้นเข้ามาได้ โดยไม่มีใครได้อ่าน changelog ก่อนเลย การ upgrade ที่ดูเหมือนเล็กน้อยอาจ rename variable, เปลี่ยนค่า default หรือปรับโครงสร้าง resource ที่สร้างขึ้น และทั้งหมดนี้จะไม่โผล่ให้เห็นจนกว่า `plan` หรือ `apply` จะเริ่มทำงานต่างจากเมื่อวาน

```hcl
# unpinned - always resolves to the latest version on init -upgrade
module "vpc" {
  source = "terraform-aws-modules/vpc/aws"
}
```

นี่คือ failure mode เดียวกับที่ provider lock file มีไว้ป้องกัน แค่ขยับขึ้นมาอีกชั้นหนึ่ง แทนที่ provider plugin จะเปลี่ยนไปโดยที่คุณไม่รู้ตัว กลายเป็น implementation ภายในของ module เองที่เปลี่ยนไปโดยที่คุณไม่รู้ตัวแทน

## pessimistic constraint operator: version = "~> 6.0"

การ pin module จาก Registry หรือ Git tag ด้วย `version` argument แก้ปัญหานี้ได้ pattern ที่ใช้กันทั่วไปคือ pessimistic constraint operator หรือ `~>`

```hcl
# pinned - allows 6.x patch and minor upgrades, blocks a 7.0 major bump
module "vpc" {
  source  = "terraform-aws-modules/vpc/aws"
  version = "~> 6.0"
}
```

`~> 6.0` แปลว่า "อนุญาต version ใดก็ได้ที่มากกว่าหรือเท่ากับ `6.0` ไปจนถึงแต่ไม่รวม `7.0`" พูดอีกแบบคือ ยอมให้ Terraform ดึง patch และ minor release ภายในสาย `6.x` ได้ — bug fix กับของใหม่ที่ backward-compatible — แต่ปฏิเสธการกระโดดไป `7.0` แบบเงียบ ๆ เพราะ major version bump คือสัญญาณที่คนเขียน module บอกว่าให้เตรียมรับ breaking change คุณยังเป็นคนเลือกเองว่าจะกระโดดตอนไหน และอ่าน changelog ของ module ก่อนได้เสมอ

## private module source กับการ publish module ของตัวเอง

ไม่ใช่ทุก module ที่ควรอยู่บน public Registry module ที่เข้ารหัส networking layout, naming convention หรือ compliance requirement เฉพาะของบริษัทหนึ่ง ไม่ควรถูก publish ให้คนทั้งโลกเห็น สำหรับกรณีนี้ องค์กรมักใช้ **private Git repository** หรือ **private module registry** — private registry ของ HCP Terraform หรือตัวที่ self-host เอง — เพื่อให้ module ยังคงมี version และค้นหาได้ภายในองค์กร โดยไม่ต้อง public เลย

```hcl
# private Git repository
module "internal_networking" {
  source = "git::ssh://git@github.com/acme-corp/terraform-modules.git//networking?ref=v2.1.0"
}

# private module registry (e.g. HCP Terraform)
module "internal_networking" {
  source  = "app.terraform.io/acme-corp/networking/aws"
  version = "~> 2.1"
}
```

ถ้าคุณต้องการ publish module ของตัวเองแบบ public จริง ๆ public Terraform Registry คาดหวัง naming convention ของ repository แบบเฉพาะ คือ `terraform-<PROVIDER>-<NAME>` เช่น `terraform-aws-vpc` Registry ใช้ชื่อนี้ในการแยกว่า module นี้เล็งไปที่ provider ไหนและควรเรียกว่าอะไร ดังนั้น repository ที่ไม่ตาม pattern นี้จะ publish ไม่ผ่าน

<Mermaid title="A pinned module version versus an unpinned one that can silently resolve to a newer release" code={moduleVersioningDiagram} />

export const moduleVersioningDiagram = `flowchart TB
  subgraph pinned["Pinned version"]
    p1["version = ~> 6.0"] --> p2["terraform init always resolves within 6.x"]
  end
  subgraph unpinned["Unpinned version"]
    u1["no version constraint"] --> u2["terraform init -upgrade may resolve 7.0"]
    u2 --> u3["breaking changes applied without changelog review"]
  end`;

<Callout title="The one-line takeaway">
module `source` ที่ไม่ pin version ไว้สามารถ resolve ไปเป็น version ใหม่กว่าเดิมที่มี breaking change ได้แบบเงียบ ๆ ในครั้งถัดไปที่ `init` รัน ดังนั้น module จาก Registry และ Git tag ควรมี `version` constraint เหมือนกับที่ provider มีใน lock file pessimistic constraint operator คือ `version = "~> 6.0"` อนุญาต patch และ minor upgrade ภายในสาย `6.x` แต่กัน major bump ไป `7.0` ไว้จนกว่าคุณจะเลือกกระโดดเอง module ที่เฉพาะกับองค์กรและไม่ควร public มักอยู่ใน **private Git repository** หรือ **private module registry** แทนที่จะอยู่บน public Registry ส่วน module ที่คุณต้องการ publish แบบ public ต้องตาม naming convention ของ repository คือ `terraform-<PROVIDER>-<NAME>` เช่น `terraform-aws-vpc`
</Callout>

export const quizModuleVersioning = [
  {
    q: 'การปล่อย module source จาก Registry หรือ Git tag ไว้โดยไม่มี version constraint ทำให้เกิดความเสี่ยงอะไร',
    options: [
      'ไม่มีความเสี่ยงเลย Terraform ใช้ version เดิมที่เคยใช้ครั้งแรกเสมอไม่ว่าจะมี constraint หรือไม่',
      'terraform init -upgrade ในอนาคต หรือ init ครั้งแรกใน CI อาจดึง version ใหม่ที่มี breaking change เข้ามาแบบเงียบ ๆ โดยไม่มีใครได้อ่าน changelog ของ module ก่อน',
      'มีผลแค่กับ output ของ terraform fmt เท่านั้น ไม่กระทบ plan หรือ apply',
      'บังคับให้ทุกคนที่ใช้ module ต้องใช้ Terraform CLI version เดียวกันเป๊ะ',
    ],
    answer: 1,
  },
  {
    q: 'version = "~> 6.0" อนุญาตอะไร และกันอะไร',
    options: [
      'อนุญาตทุก version รวมถึง 7.0 ขึ้นไปโดยไม่มีข้อจำกัด',
      'อนุญาตเฉพาะ version 6.0 เป๊ะ ๆ เท่านั้น',
      'อนุญาต patch และ minor upgrade ในสาย 6.x แต่กัน major version bump ไปเป็น 7.0',
      'กัน upgrade ทุกอย่างจนกว่าจะเอา constraint ออกเอง',
    ],
    answer: 2,
  },
  {
    q: 'ทำไมองค์กรอาจใช้ private Git repository หรือ private module registry แทน public Terraform Registry สำหรับ module หนึ่ง',
    options: [
      'เพราะ private source ดาวน์โหลดเร็วกว่า public Registry module เสมอ',
      'เพราะ module นั้นเข้ารหัส infrastructure detail เฉพาะของบริษัทที่ไม่ควร publish ออกไป แต่ยังต้องมี version และค้นหาได้ภายในองค์กร',
      'เพราะ public Terraform Registry ไม่รองรับ version argument',
      'เพราะ private source ไม่ต้องมี source argument ใน module block',
    ],
    answer: 1,
  },
  {
    q: 'public Terraform Registry คาดหวัง naming convention ของ repository แบบไหนสำหรับการ publish module ของตัวเอง',
    options: [
      'any-name-you-want-aws',
      'terraform-<PROVIDER>-<NAME> เช่น terraform-aws-vpc',
      '<NAME>-terraform-module',
      'aws-terraform-<NAME>',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="modules/module-versioning-and-registries" questions={quizModuleVersioning} />

<ProgressTracker client:visible id="modules/module-versioning-and-registries" />
